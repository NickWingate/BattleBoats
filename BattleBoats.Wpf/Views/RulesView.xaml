<UserControl x:Class="BattleBoats.Wpf.Views.RulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BattleBoats.Wpf.Views"
             xmlns:nav="clr-namespace:BattleBoats.Wpf.Services.Navigation"
             xmlns:buttons="clr-namespace:BattleBoats.Wpf.Styles.Buttons"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid>
        <TextBlock Text="rules" FontSize="200"/>
        <buttons:GradientButton Content="Back" Foreground="White" FontSize="30"
                                Command="{Binding UpdateCurrentViewModelCommand}"
                                CommandParameter="{x:Static nav:ViewType.Menu}"
                                GradientColor0="Orange"
                                GradientColor1="teal"
                                GradientColor2="Aqua"
                                CornerRadius="25"
                                Width="500"
                                Height="50"
                                VerticalAlignment="Bottom"/>

    </Grid>
</UserControl>
